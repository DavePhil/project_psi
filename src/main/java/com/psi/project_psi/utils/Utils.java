package com.psi.project_psi.utils;


import com.psi.project_psi.models.Admin;
import org.springframework.data.repository.CrudRepository;
import org.springframework.http.ResponseEntity;
import org.springframework.stereotype.Repository;

import java.util.Optional;

public class Utils {

//    public ResponseEntity<String> delete (Long id, CrudRepository repository){
//        Optional<?> deleteObject = repository.findById(id);
//        if (deleteObject.isPresent()) re
//    }

}
